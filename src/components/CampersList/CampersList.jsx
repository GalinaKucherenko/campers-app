export default function CampersList(campers) {
    return (
        <div>
            <p>List</p>
          {/* <ul>
            {campers.map(({ objectID, name, price }) => (
              <li key={objectID}>
                <p>{name}</p>
                <p>{price}</p>
              </li>
            ))}
          </ul> */}
      </div>
    )
}